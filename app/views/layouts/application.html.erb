<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pokeapi-Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pokeapi-Rails">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <script>
      (function() {
        let key = "pokeapi-theme";
        let stored = null;
        try { stored = localStorage.getItem(key); } catch (_e) {}
        let isDark = stored ? stored === "dark" : (window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches);
        if (isDark) document.documentElement.classList.add("dark");
      })();
    </script>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body data-controller="theme">
    <header class="site-header">
      <div class="site-header-inner">
        <p class="site-brand">PokeAPI Rails</p>
        <nav class="site-nav">
          <button type="button" data-action="theme#toggle" data-theme-target="toggle" class="theme-toggle" aria-label="Switch theme" title="Switch theme">
            <svg xmlns="http://www.w3.org/2000/svg" data-theme-target="moon" class="theme-icon theme-icon-moon lucide lucide-moon" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20.985 12.486a9 9 0 1 1-9.473-9.472c.405-.022.617.46.402.803a6 6 0 0 0 8.268 8.268c.344-.215.825-.004.803.401"/>
            </svg>
            <svg xmlns="http://www.w3.org/2000/svg" data-theme-target="sun" class="theme-icon theme-icon-sun lucide lucide-sun" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/>
            </svg>
            <span data-theme-target="label" class="sr-only">Switch to Dark</span>
          </button>
          <a href="/api/v2/">API v2 Root</a>
          <a href="/api/v3/">API v3 Root</a>
        </nav>
      </div>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer class="site-footer">
      Code is licensed under the BSD 3-Clause License. Inspired by the upstream PokeAPI project.
      Pokemon and Pokemon character names are trademarks of Nintendo.
    </footer>
  </body>
</html>
